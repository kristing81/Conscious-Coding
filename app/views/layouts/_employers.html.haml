%nav.navbar-fixed-top{role: "navigation"}
  .container
    .navbar-header
      %button.navbar-toggle{"data-target" => "#collapse", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Menu
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
    #collapse.collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li.header= link_to "Conscious Coding", root_path
      %ul.nav.navbar-nav.navbar-right
        %li= link_to "About", about_path
        %li.dropdown{role: "presentation"}
          %a.dropdown-toggle{"aria-expanded" => "true", "data-toggle" => "dropdown", href: "#", :role => "button"}
            Jobs
            %span.caret
          %ul.dropdown-menu{"aria-labelledby" => "dropdownMenu", :role => "menu"}
            %li= link_to "All Jobs", jobs_path
            %li
            Jobs by Category
            - Category.all.each do |cat|
              %li= link_to cat.name, category_path(cat)
            %li= link_to "Explore Organizations", employers_path
            %li.dropdown{role: "presentation"}
              %a.dropdown-toggle{"aria-expanded" => "true", "data-toggle" => "dropdown", href: "#", :role => "button"}
                Employers
                %span.caret
              %ul.dropdown-menu{role: "menu"}
                %li= link_to "Employer Sign up", new_employer_registration_path   
                %li= link_to "Employer Login", new_employer_session_path     
                %li= link_to "Post a Job", new_job_path 
            .pull-right.employer-info
            - if employer_signed_in?
              Hello #{current_employer.company_name}! #{link_to "Sign out", destroy_employer_session_path, method: :delete}
            - else
              = link_to "Employer Sign Up", new_employer_registration_path