= form_tag(jobs_path, :method => "get", class: 'form-horizontal', id: "search-form") do
  .input-append
    = text_field_tag :search, params[:search], class: "form-control", placeholder: "Search by title, position, skills, keywords"
    %button.btn.btn-default{:type => "submit"}
      %i.glyphicon.glyphicon-search





/ = simple_form_for :search, html: { class: 'form-horizontal'}, :method => :get  do |f|

/   = f.input :search, as: :search,:id="autocomplete" ,placeholder: "Search by title, position, skills, keywords"
/   %br/
/   = f.button :submit, :name => nil,  class: "btn btn-default"

/ = link_to_function "Clear", "$('autocomplete').clear()"
/   =f.input :search
/   =f.submit "Search"